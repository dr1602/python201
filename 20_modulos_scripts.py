# los modulos en python se pueden correr de dos formas
# declarando las funciones en el moduylo como en utils .py e importando desde otro archivo
# tambien se puden correr estos archivos de forma directa como scripts
# la udalidad puede causra errores, al correr archivo como modulo pero tambier correr como script eso puede causar conflicto,
# para adecuar nuestro archivo para correrlo importando o desde la terminal, es asi

#por ejemplo main.py se puede ejecutar un scirpt, y se tiene un resultado.
# main.py tambien es considerado como modulo

# como aqui
